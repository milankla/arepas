# ── Data pipeline ─────────────────────────────────────────────────────────────
pandas
numpy<2               # NumPy 2.0 compatibility fix
Pillow
loguru

# ── ML training stack ─────────────────────────────────────────────────────────
# PyTorch: CPU + MPS (Apple Silicon) on macOS; add --index-url for CUDA on Linux
torch
torchvision
timm                  # EfficientNet-B3 and ViT-B/16 backbone support
scikit-learn          # stratified train/val/test splits, metrics

# ── Config & utilities ────────────────────────────────────────────────────────
pyyaml                # model_config.yaml loading in train_multi_task.py
tqdm                  # training progress bars

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib            # class distribution charts, confusion matrices

# ── Building detection (GroundingDINO via HuggingFace Transformers) ────────────
transformers          # GroundingDINO: AutoModelForZeroShotObjectDetection
accelerate            # optional but recommended for transformers inference

# ── API server (Arepas UI backend) ────────────────────────────────────────────
fastapi
uvicorn[standard]     # ASGI server with auto-reload support

# ── Cloud storage (deployment Phase 0 storage abstraction) ────────────────────
boto3                 # S3 backend, used only when AREPAS_S3_BUCKET is set
moto                  # dev/test: in-memory S3 for the storage parity tests

# ── Auth (deployment Phase 2) ─────────────────────────────────────────────────
pyjwt[crypto]         # Cognito RS256 JWT verification; [crypto] pulls in cryptography
